import { Router } from "express";
// import { createSlot } from "../controllers/slotController.js";

const slotRouter = Router();

// slotRouter.post("/create/:serviceId", createSlot);

export default slotRouter;
